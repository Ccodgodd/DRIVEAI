// Main JavaScript for common UI interactions

document.addEventListener('DOMContentLoaded', () => {
    // Add mobile menu toggle logic if needed later
    // Initialize feather icons
    if (typeof feather !== 'undefined') {
        feather.replace();
    }
});
